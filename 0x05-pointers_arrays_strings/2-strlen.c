#include "main.h"

/**
 * _strlen - returns lenth of string
 *
 * @s: input of the string
 *
 * Return: lennth of string
 *
 */

int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0'; index++)
	{
		;
	}
	return (index);
}
